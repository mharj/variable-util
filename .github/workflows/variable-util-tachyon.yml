name: variable-util-tachyon to npmjs
on:
  push:
    branches: [main]
    paths: 
      - "packages/variable-util-tachyon/**"
      - ".github/workflows/variable-util-tachyon.yml"
      - "package.json"
  pull_request:
    branches: [main]
    paths: 
      - "packages/variable-util-tachyon/**"
      - ".github/workflows/variable-util-tachyon.yml"
      - "package.json"

permissions:
  id-token: write # Required for OIDC
  contents: read

jobs:
  module-flow:
    uses: ./.github/workflows/module.yml
    secrets: inherit
    with:
      package-path: packages/variable-util-tachyon